# D-I config version 2.0
# search path for the c32 support libraries (libcom32, libutil etc.)
PATH debian-installer/amd64/boot-screens/
UI vesamenu.c32

DEFAULT reimage 
TIMEOUT 5 

LABEL reimage
    MENU LABEL ^New Cluster
    KERNEL debian-installer/amd64/linux
    APPEND auto=true priority=critical vga=788 initrd=debian-installer/amd64/initrd.gz ipv6.disable=1 preseed/url=tftp://raspberrypi/preseed-k8s-leader.cfg

LABEL local
    MENU LABEL Boot local hard drive
    LOCALBOOT 0

